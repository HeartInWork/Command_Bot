module.exports.run = async (bot, message, args) => {
    console.log("the mute command has been run.");

}

module.exports.help = {
    name: "mute",
    descriptor: "Mute a member. (Command not working yet)"
}